# 📖 编程学习与踩坑笔记 (Learning Notes)

> **写在前面**：
> 这是一份写给“编程小白”也能看懂的项目经验总结。在开发这个预约网站（特别是“趣味贴纸”和“弹窗”等功能）的过程中，我们遇到了很多有趣的挑战。
> 以后每次遇到新的 Bug 和学到的新知识，都会用大白话记录在这里！

---

## 🚀 一、 如何让网页“飞”起来？（性能优化篇）

### 1. 为什么拖拽贴纸一开始会卡成 PPT？
*   **小白大白话**：在 React（我们用的网页框架）里，有一个机制叫“状态更新（State）”。想象 React 是一个非常严谨的画师，如果你告诉他：“贴纸往右移动了 1 毫米”，他就会把整个页面重新审视一遍，然后再画出来。当你拖拽鼠标时，一秒钟会产生 60 次移动，画师直接累趴下，网页就卡顿了。
*   **怎么优化的**：我们“绕过”了画师，使用了 `ref`（直接操作底层元素）的方法。相当于我们自己偷偷把贴纸挪过去，不通知画师重绘整个页面。这样拖拽就如丝般顺滑了（60fps）。
*   **经验总结**：**高频触发的动作（如拖拽、滚动），千万不要用 React 的 `setState` 去更新坐标，一定要直接操作 DOM（底层元素）的 `style`。**

### 2. 为什么贴纸有时候要等一会儿才弹出来？
*   **小白大白话**：就像你看在线视频，如果不缓冲，点开就会转圈圈。图片也是一样，只有当你把贴纸拖出来的那一刻，浏览器才去慢吞吞地下载图片，所以会有延迟。
*   **怎么优化的**：我们加了“预加载（Preload）”。在网页刚打开、用户还没点日历的时候，系统就在后台悄悄把所有贴纸图片下载到浏览器缓存里了。等用户一点击，直接“秒出”。
*   **经验总结**：**对于需要瞬间出现的图片（比如特效、表情包、贴纸），一定要在代码初始化时提前 `new Image().src = "..."` 把它们加载好。**

---

## 🛡️ 二、 如何避免元素打架？（交互冲突篇）

### 1. 手机上长按贴纸，为什么总是触发系统的“选中文本”或“保存图片”？
*   **小白大白话**：手机浏览器有一种“本能反射”，觉得你长按屏幕就是想复制文字或者保存图片。
*   **怎么优化的**：我们给贴纸加了几句“镇咒”一样的 CSS 样式代码，比如 `user-select: none`（不许选文字）、`-webkit-touch-callout: none`（不许呼出系统菜单）、`touch-action: none`（不许触发屏幕滚动）。
*   **经验总结**：**做网页端的拖拽、游戏或长按交互时，必须先用 CSS 禁用掉浏览器的“默认本能”。**

### 2. 为什么放大的贴纸会被外面的框框“切掉”一半？
*   **小白大白话**：为了排版好看，很多外层盒子会设置 `overflow: hidden`（超出盒子范围的东西直接切掉隐藏）。当你把贴纸拖到盒子边缘，或者贴纸放大时，超出的部分就像被剪刀剪掉了一样。
*   **怎么优化的**：我们用了一招叫“传送门（React Portals）”的魔法。在贴纸被拖起的那一刻，我们把它从原来的盒子里“传送”到了网页的最顶层（`document.body`）。这样它就不受任何盒子的限制，想飞去哪就飞去哪。
*   **经验总结**：**弹窗、拖拽元素、下拉菜单，遇到被遮挡或被切掉的问题，直接用 `createPortal` 把它们丢到最外层（Body）去渲染。**

---

## 🐛 三、 Bug 修复记录库（打怪升级日志）

### 🐛 Bug 1：垃圾桶变成了“无差别黑洞”
*   **症状**：以前只要把贴纸拖到屏幕最底下任意位置，贴纸就被删了，容易误触。
*   **修复**：我们让系统去获取垃圾桶图标的“真实屏幕坐标（BoundingClientRect）”，然后计算鼠标是不是真的悬停在垃圾桶的那个方块里（加了一点点周围的容错范围）。只有精确投篮，才算删除。

### 🐛 Bug 2：拖拽贴纸过程中的“大变活人”
*   **症状**：拖起贴纸 A，原来的位置刷新出了贴纸 B，结果手里拖着的 A 也变成了 B。放下后又变回了 A。
*   **修复**：这是因为 React 刷新时把手里拖着的图片链接也更新了。我们在鼠标按下的那一刻，给图片链接拍了个“快照（ref）”锁死。拖拽期间，不管底下的空位怎么变，手里拿的永远是快照里的那张图。

### 🐛 Bug 3：“找我耍”标题的弹跳动画神秘消失
*   **症状**：为了让图标在暗色模式下变色，加了一层蒙版（Mask），结果收起日历时图标不跳了。
*   **修复**：因为动画绑定的“标签（ref）”还在旧的隐藏图片上，肉眼看的是新加的蒙版。把绑定动画的标签移到它们共同的“父级大盒子”上，带着盒子一起跳，问题解决。

### 🐛 Bug 4：按钮误触直接跳转
*   **症状**：在滚动屏幕时，如果不小心点到了“看小鸟”这种跳转按钮，会直接跳走，体验很差。
*   **修复**：给按钮加了“两段式确认”。第一次点击只是“选中”并显示“去抖音”的提示，2秒内再点一次才真跳转。既防误触，又增加了交互反馈。

---

## 🛠️ 四、 小白怎么排查和测试问题？

如果你以后自己想改点东西，遇到问题可以按这个套路来：

1.  **分层检查（层级不对 / 东西看不见）**
    *   在网页上右键 -> 检查（Inspect）。看看元素是不是在 DOM 树里。
    *   如果在，但看不见，多半是 `z-index`（层级）不够大被别人盖住了，或者是被父级的 `overflow: hidden` 切掉了。
2.  **样式问题（颜色不对 / 没居中）**
    *   不要直接改代码，先在浏览器的“开发者工具（F12）”里，找到那个元素，在右边的 `Styles` 面板里直接改数字、改颜色。调到满意了，再把代码复制回项目里。
3.  **动画卡顿（性能测试）**
    *   在手机上测试才是最真实的！电脑浏览器性能太强，有时候掩盖了卡顿。
    *   如果在手机上卡，找找看是不是在动画里改变了 `width`, `height`, `left`, `top`。**记住：动画只用 `transform`（平移、缩放）和 `opacity`（透明度），这俩是唯一不卡顿的魔法属性！**
4.  **保留后悔药（注释代码）**
    *   不确定的代码不要直接删，用 `//` 或者 `{/* */}` 把它注释掉。就像我们换抖音链接一样，旧链接注释在旁边，随时可以复原。